# Imagen usada solo como transporte para respaldar/restaurar ./cache
# (audio, base de datos, etc). No corre nada, solo empaqueta el contenido.
FROM scratch

COPY cache/audio/ /cache/audio/
COPY cache/database/ /cache/database/

CMD ["true"]
